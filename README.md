# rc_car
Run the mobile app:
1. cd mobile
2. npm install
3. npx expo start
4. scan the QR code from the Expo Go iOS app

Run the arduino:
1. open a file in the /arduino_controller_sketch folder
2. upload an .ino file